# 修改 Bug

## 2026-04-12：网站 Markdown 渲染缺少斜体支持

**问题**：信件详情页中，Markdown 的斜体语法 `*文字*` 没有被转换为 HTML，直接显示了星号。比如 2024 年那篇里的 `*报告*` 显示成了原始的 "这封信是伯克希尔年度*报告*的一部分。"

**原因**：`website/index.html` 的 `renderMarkdown()` 函数处理了 `**粗体**`，但遗漏了 `*斜体*`。

**修复**：在粗体处理之后增加斜体转换：

```javascript
// 粗体
text = text.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
// 斜体
text = text.replace(/\*(.+?)\*/g, '<em>$1</em>');
```

注意顺序：必须先处理 `**`（粗体），再处理 `*`（斜体），否则粗体的两个星号会被斜体规则错误匹配。
