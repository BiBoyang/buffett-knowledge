# Level 2 学习日志：高级检索

> 日期：2026-04-08
> 前置：Level 1 基础 RAG 已完成（分块→Embedding→Chroma→DeepSeek问答）

---

## Level 2 要解决什么问题？

Level 1 的向量检索有一个根本缺陷：

```
问题: "伯克希尔1985年的浮存金是多少？"
                    ↓
向量检索找到的是: "语义相似"的段落
但: "1985" 只是一个数字，语义上跟 "1986" 几乎没区别
    向量检索可能返回 1984 或 1986 年的内容

问题: 关键词精确匹配和语义相似是两个不同的需求
```

Level 2 的三个技术分别解决：

| 技术 | 解决什么问题 |
|------|-------------|
| **混合检索** | 向量找"语义相关"，BM25找"关键词精确匹配"，两者取长补短 |
| **Reranking** | 初步检索 Top-50 粗筛，Cross-Encoder 精排 Top-5 |
| **查询改写** | 用户的问题不一定能直接检索到好结果，需要改写/扩展/拆分 |

---

## 技术选型记录

### 2.1 混合检索

**背景**：向量检索擅长语义匹配但不擅长精确关键词匹配，BM25 擅长关键词但不理解语义。

**方案**：
- 向量检索：Chroma（已有）
- BM25 检索：用 `rank_bm25` 库（纯 Python，最简单）
- 融合策略：**RRF（Reciprocal Rank Fusion）**

**RRF 原理**：
```
假设向量检索排名: [A, B, C, D, E]
假设 BM25 排名:   [C, A, E, F, G]

RRF 给每个文档打分:
  score(d) = 1/(k + rank_vector(d)) + 1/(k + rank_bm25(d))
  k 通常 = 60

  A: 1/(60+1) + 1/(60+2) = 0.0164 + 0.0161 = 0.0325
  C: 1/(60+3) + 1/(60+1) = 0.0159 + 0.0164 = 0.0323
  ...

最终排序: A > C > ... （两路都靠前的排在前面）
```

**为什么用 RRF 而不是简单加权**：
- RRF 只依赖排名，不依赖原始分数 → 两路检索分数尺度不同也不影响
- 简单加权需要归一化两路的分数，容易出错

### 2.2 Reranking

**背景**：向量检索用的 Embedding 是"双编码器"（Bi-Encoder），query 和 document 分别编码再比较 → 快但不够精确。

**方案**：`Qwen3-Reranker-0.6B`（阿里 Qwen 系列，中英双语）

> 注：最初使用了 bge-reranker-v2-m3，后统一切换为 Qwen 系列

**Cross-Encoder 原理**：
```
Bi-Encoder（Embedding，快但粗）:
  query → [0.1, 0.3, ...] ──→ cos 相似度 ←── doc → [0.2, 0.4, ...]
  query 和 doc 分别编码，不交互

Cross-Encoder（Reranker，慢但准）:
  [query + SEP + doc] → 一个分数
  query 和 doc 拼在一起同时编码，能理解两者关系

为什么更准？因为 Cross-Encoder 能看到 query 和 doc 的交互
为什么不能替代 Embedding？因为慢，不能用来做全量检索
所以：先 Embedding 粗筛 Top-50，再 Cross-Encoder 精排 Top-5
```

### 2.3 查询改写

**HyDE（Hypothetical Document Embedding）原理**：
```
用户: "巴菲特怎么看通胀"
  ↓
Step 1: 让 LLM 假想一个答案（可能是错的）
  "巴菲特认为通胀是投资者最大的敌人..."
  ↓
Step 2: 用这个假想答案去向量检索
  ↓
Step 3: 检索到的真实文档比直接用问题检索更相关

为什么有效？
因为 "答案" 跟 "文档" 在向量空间中更近
而 "问题" 跟 "文档" 可能距离较远
```

**Multi-Query 原理**：
```
用户: "巴菲特怎么看护城河"
  ↓
改写为3个不同角度的查询:
  Q1: "巴菲特护城河的定义和标准"
  Q2: "伯克希尔旗下公司的竞争优势"
  Q3: "护城河在投资决策中的作用"
  ↓
分别检索 → 合并去重 → 更全面的召回
```

---

## 实验记录

### 2026-04-08：混合检索实验

**测试 Query: "伯克希尔1985年的浮存金"**

| 模式 | Top-1 结果 | 分析 |
|------|-----------|------|
| 纯向量 | 2020年信（错年份！） | 语义相关但年份不匹配 |
| 纯 BM25 | 2001年信 | 关键词命中"浮存金"，但也不是1985 |
| **混合** | 2001年信 | 融合了两路结果，比单一检索好 |

**结论**：混合检索取长补短，向量找语义相关的，BM25 找关键词匹配的，RRF 融合。

### 2026-04-08：Reranking 实验

**测试 Query: "什么是安全边际？巴菲特如何应用？"**

| | 重排前 | 重排后 |
|--|--------|--------|
| Top-1 | 1992年信（rrf=0.0323） | **1990年信（rerank=0.3770）** |
| Top-2 | 1962年信（rrf=0.0309） | 1962年信（rerank=0.1609） |

**关键发现**：1990年信直接引用了格雷厄姆《聪明的投资者》关于安全边际的原文，是最相关的结果。但混合检索只排到第4，Reranker 把它提到了第1。

**为什么 Reranker 能做到**：Cross-Encoder 同时看 query 和 document，能理解"安全边际"跟"格雷厄姆"的深层关系，而 Embedding 检索做不到这一点。

### 2026-04-08：查询改写实验

**测试 Query: "巴菲特怎么看通胀"**

| 策略 | Top-1 结果 | 分析 |
|------|-----------|------|
| 原始查询 | 1984年信（WPPSS债券） | 不太相关 |
| **HyDE** | 1983年信（通胀与有形资产） | 明显更好！ |
| Multi-Query | 1981年信 + 1969年信 + 1987年信 | 最全面，覆盖多个年代 |

**HyDE 为什么有效**：
- LLM 假想答案的风格跟真实文档相似 → 向量空间中距离更近
- 假想答案包含"通胀""定价权""生产性资产"等关键词 → 检索更精准
- 即使假想答案的事实不完全正确，检索方向是对的

**Multi-Query 为什么有效**：
- 把一个问题拆成多个角度 → 分别命中不同年份的内容
- 合并去重后覆盖面更广 → 回答更全面

### Level 2 总结

| 技术 | 解决的问题 | 效果提升 | 额外成本 |
|------|-----------|---------|----------|
| 混合检索 | 精确关键词匹配 | 中等 | BM25索引（内存） |
| Reranking | 排序精度 | 显著 | 560MB模型 + 推理时间 |
| HyDE | 语义检索召回率 | 中等 | 1次LLM调用 |
| Multi-Query | 召回全面性 | 显著 | N次LLM调用 + N次检索 |

**学到的**：
1. 没有单一检索策略是万能的，混合才是王道
2. Reranking 的提升是"免费的午餐"——几乎不影响体验但质量提升明显
3. 查询改写是一把双刃剑：HyDE 可能带偏方向，Multi-Query 增加成本
4. Level 2 的完整流程：query → 改写 → 混合检索 Top-20 → Rerank Top-5 → LLM
