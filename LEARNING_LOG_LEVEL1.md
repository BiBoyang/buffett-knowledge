# Buffett RAG 学习项目日志

> 用巴菲特致股东信（99篇，3.8MB 中文 Markdown）学习 RAG 全链路技术。
> 数据来源：`/Users/boyang/Downloads/巴菲特致股东信`

---

## 目录

- [项目目标](#项目目标)
- [技术选型记录](#技术选型记录)
- [学习进度](#学习进度)

---

## 项目目标

用同一份数据，从 Level 1 到 Level 4 逐步搭建 RAG 系统：

- **Level 1**：基础 RAG（分块 → Embedding → 向量库 → 问答）
- **Level 2**：高级检索（混合检索 + Reranking + 查询改写）
- **Level 3**：GraphRAG（知识图谱抽取 + 图检索）
- **Level 4**：评估 + 工程化（Ragas 评测 + 静态网站生成）

---

## 技术选型记录

### 2026-04-07：Embedding 模型选型

**背景**：需要选择一个 embedding 模型将巴菲特信件转为向量。数据是中文，所以需要中英双语支持。

**候选方案**：

| 模型 | 参数量 | 上下文 | 中文MTEB | 是否免费 | 更新时间 |
|------|--------|--------|----------|----------|----------|
| bge-m3 | 568M | 8K | 中上 | 免费 | 2024 |
| **Qwen3-Embedding-0.6B** | 0.6B | **32K** | **最强** | 免费 | 2026 |
| jina-embeddings-v3 | 570M | 8K | 中上 | 免费 | 2024 |
| OpenAI text-embedding-3 | API | 8K | 强 | 付费 | 2024 |
| Gemini Embedding 2 | API | 8K | 英文最强 | 免费额度 | 2026 |

**决定**：Qwen3-Embedding-0.6B（本地运行）

**理由**：
1. 中文 MTEB 基准测试当前最强，显著超越 bge-m3
2. 0.6B 参数量，Mac 本地就能跑，不需要 GPU
3. 32K 上下文是关键优势——巴菲特的信件很多超过 8K token，bge-m3 会截断
4. 可调输出维度（32~2560），方便后续实验维度对检索效果的影响
5. 纯开源免费，不依赖外部 API

**潜在风险**：
- 0.6B 是 Qwen3-Embedding 系列最小的，如果效果不够好可以升级到 4B
- 本地跑需要下载约 1.2GB 模型文件

**更新（2026-04-12）**：已从 `sentence-transformers`（PyTorch）切换到 `mlx-embeddings`（MLX），在 Apple Silicon 上获得约 3 倍的推理加速，数值差异 <0.003，可忽略。

**学到的**：
- Embedding 模型更新很快，bge-m3 曾经是王者但已经被超越
- 选择 embedding 要看：语言支持、上下文长度、参数量（能否本地跑）、MTEB 得分
- 上下文长度很重要——如果文档超过模型的 max_length，会被截断，丢失信息

---

### 2026-04-07：LLM 选型

**背景**：需要 LLM 做最终的回答生成（Reader 部分）。

**候选方案**：

| 模型 | 中文能力 | 价格 | 特点 |
|------|----------|------|------|
| **DeepSeek Chat** | 强 | ¥1/百万token | OpenAI 兼容接口，便宜 |
| Claude | 很强 | 较贵 | 长文本理解好 |
| GPT-4o | 强 | 贵 | 综合强 |
| Qwen3 本地 | 强 | 免费 | 需要大显存 |

**决定**：DeepSeek API

**理由**：
1. OpenAI 兼容接口，代码可以直接用 `openai` 库，换模型只改 base_url
2. 价格极低，学习阶段随便调
3. 中文能力强
4. 后续如果想换其他模型，只需改配置

**学到的**：
- LLM 在 RAG 中的作用是 "Reader"——根据检索到的上下文生成回答
- OpenAI 兼容接口是个行业标准，DeepSeek、Qwen、Moonshot 都支持
- RAG 的效果 = 检索质量 × LLM 能力，两者都很重要

---

### 2026-04-07：向量数据库选型

**候选方案**：

| 数据库 | 特点 | 适合场景 |
|--------|------|----------|
| **Chroma** | 最简单，Python 原生，内置 embedding | 学习、原型 |
| Qdrant | Rust 写的，性能好，有 Web UI | 中等规模 |
| Milvus | 分布式，生产级 | 大规模生产 |
| pgvector | PostgreSQL 扩展 | 已有 PG 的项目 |
| FAISS | Meta 出品，纯库，最快 | 不需要持久化的场景 |

**决定**：Chroma

**理由**：
1. 学习项目，越简单越好
2. `pip install chromadb` 就能用，不需要启动服务
3. 数据持久化到本地文件夹
4. 后续可以轻松迁移到 Qdrant 或 pgvector

**学到的**：
- 向量数据库的核心操作就三个：insert（存向量）、query（相似度搜索）、delete
- Chroma 默认用的距离函数是 L2（欧氏距离），也可以选 cosine
- 生产环境一般不用 Chroma，但学习够了

---

## 学习进度

### Level 1：基础 RAG

- [x] 1.1 项目搭建（venv + 依赖）
- [x] 1.2 文本分块实验
- [x] 1.3 Embedding 向量化
- [x] 1.4 存入 Chroma 向量库
- [x] 1.5 检索 + LLM 问答
- [ ] 1.6 对比不同分块策略的效果

### Level 2：高级检索

- [ ] 2.1 混合检索（向量 + BM25）
- [ ] 2.2 Reranking
- [ ] 2.3 查询改写

### Level 3：GraphRAG

- [ ] 3.1 实体和关系抽取
- [ ] 3.2 知识图谱构建
- [ ] 3.3 图检索

### Level 4：评估 + 工程化

- [ ] 4.1 RAG 评估（Ragas）
- [ ] 4.2 静态网站生成

---

## 数据概况

```
路径：/Users/boyang/Downloads/巴菲特致股东信
文件数：99 篇 Markdown
总大小：3.8MB
分类：
  - 伯克希尔股东信：60 篇 (1965-2024)
  - 合伙人信：36 篇 (1956-1970)
  - 特别信件：3 篇

数据特点：
  - 已有双向链接（[概念](../concepts/xxx.html)）
  - 中文翻译文本
  - 包含表格数据（财务数据）
  - 单篇长度差异大（几千到几万字符）
```

---

## Level 1 实验记录

### 2026-04-07：分块实验结果

三种策略对比（99篇文档）：

| 策略 | 分块数 | 平均大小 | 最小 | 最大 | 特点 |
|------|--------|----------|------|------|------|
| 固定大小(500字) | 3613 | 492 | 1 | 500 | 大小均匀，但会切断句子 |
| 段落分块(max=800) | 2506 | 655 | 1 | 800 | 语义完整，大小不均 |
| **递归分块(500字)** | **4310** | **370** | 1 | 500 | 在语义边界切分，最终选用 |

**决定用递归分块**：
- 按标题→段落→句子→字符的优先级切分，尽量不破坏语义
- 4310个分块 → 1024维向量 → Chroma 余弦相似度检索

### 2026-04-07：首次 RAG 问答测试

**问题1：巴菲特怎么看护城河？**
- 检索到 2007年、2016年、2015年、1992年信件中的相关段落
- 相似度最高 0.688，最低 0.579
- 回答质量：好。引用了多个年份，区分了护城河的定义、形式、持久性
- 耗时：约10秒（embedding加载+检索+LLM生成）

**问题2：伯克希尔为什么关闭纺织业务？**
- 检索到 2014年、1985年、1969年、2021年信件
- 相似度最高 0.776（比问题1高，因为更具体）
- 回答质量：优秀。总结了5个原因，按时间线引用了4个年份
- 关键发现：问题越具体，检索相似度越高

**学到的**：
1. 相似度 0.5+ 就能检索到相关内容，0.7+ 就很精准
2. 具体问题比抽象概念检索效果更好
3. 递归分块保留了段落结构，检索时能拿到完整上下文
4. RAG 的回答质量 = 检索召回率 × LLM 理解能力，缺一不可
